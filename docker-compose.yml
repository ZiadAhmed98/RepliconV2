version: '3.8'

services:
  app:
    build: .
    container_name: mds_dashboard
    restart: always
    ports:
      # This lets people visit your IP normally (port 80), 
      # and sends them to your Node server (port 3000)
      - "80:3000"
    env_file:
      - .env